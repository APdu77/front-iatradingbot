import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.crypto',
  appName: 'front-iatradingbot2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
